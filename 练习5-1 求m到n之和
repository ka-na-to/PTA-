#include<stdio.h>
int main (void)
{
    int m,n,i,sum = 0;
    printf("please input m,n:");
    scanf("%d,%d",&m,&n);
    for(i = m;i <= n;i++){        //   这个地方重新令了一个 i 实在是妙
      sum = sum+ i ;
    }
	 printf("%d\n",sum);     //注意printf的位置，放在for循坏内部就会被执行多次
}
